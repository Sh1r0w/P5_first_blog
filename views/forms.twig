{% macro input(title, name, value, type) %}

<label for="{{ name }}"> {{ title }}</label></br>

<input type="{{ type }}" name="{{ name }}" value="{{ value }}">

{% endmacro %}

{% macro textarea(title, name, rows, cols) %}
    <label for="{{ name }}"> {{ title }}</label></br>

    <textarea name="{{ name }}" rows="{{ rows }}" cols="{{ cols }}"></textarea>
    
{% endmacro %}