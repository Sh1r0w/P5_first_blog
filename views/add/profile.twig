{% if u.img != null%}
{% block head %}<title>Profil de {{ u.firstname }} </title>{% endblock %}
    <img src="{{ u.img }}" class="card-img-top" alt=" {{ u.lastname }}" style="width: 250px">
  {% endif %}
      <h4>
            Nom : {{ u.lastname }} </br>
            Prénom : {{ u.firstname }}</br>

            Citation : {{ u.citation }}</br>
            {% if u.pdf != null %}
            Mon CV :</br>
            <embed src ="{{ u.pdf }}" width="250" height="350" type="application/pdf" />
            {% endif %}
      </h4>
      
  </br>
</br>
