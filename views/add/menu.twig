<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">
            Home
          </a>
        </li>
        {% if session.logged_user != null and session.idUs != null %}
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="posts">
              Posts
            </a>
          </li>
        {% endif %}
      </ul>
      <ul>
        <li class="nav-item">
          {% if session.logged_user == null %}
            <a class="btn btn-primary" data-bs-toggle="modal" href="#login" role="button">
              Connection / Creation
            </a>
          {% else %}
            <li class="nav-item dropdown">
              <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                {% if session.firstname == null %}
                  {{ session.idUs}} Anonyme
                {% else %}
                {{ session.firstname }} {{ session.lastname }}
                {% endif %}
                </button>
              <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark">
                  <li>
                    <a class="nav-link active" aria-current="page" href="user">
                      Mon Profil
                    </a>
                  </li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <a class="nav-link active" href="?action=logout">
                      Deconnection
                    </a>
                  </li>
              </ul>
            </li>           
          {% endif %}
        </li>
      </ul>
    </div>
  </div>
</nav>
